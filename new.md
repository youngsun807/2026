# KVTC: KV Cache Transform Coding
### LLM 추론의 메모리 병목을 해결하는 압축 기술
Konrad Staniszewski & Adrian Łańcucki (NVIDIA / University of Warsaw) | ICLR 2026

---

## 목차

1. 문제 정의: 왜 KV Cache가 병목인가?
2. 기존 해결책과 한계
3. KVTC란 무엇인가?
4. 핵심 발견: 헤드 간 숨겨진 중복성
5. KVTC 동작 원리 (3단계 파이프라인)
6. 스마트 예외 처리: Sink & Sliding Window
7. 실험 설계
8. 실험 결과
9. 지연시간(Latency) 분석
10. AI 메모리 산업에 미치는 영향
11. 한계 및 향후 연구
12. 최종 요약 및 의의

---

## 1. 문제 정의: 왜 KV Cache가 병목인가?

1. **KV Cache란?**
   - AI와 대화할 때, 눈에 보이지 않는 곳에서 메모리가 엄청난 양을 차지
   - Transformer 구조는 이전 토큰들의 Key/Value를 매번 다시 계산하지 않고 저장해 둠
   - 이 저장소가 KV Cache → AI의 단기 작업 기억장치
   - 한 번 계산한 정보를 저장해 반복 계산을 피하는 똑똑한 기능
   - 모델 파라미터(뇌 자체)와는 별개로, 지금 작업 중인 정보를 담는 임시 공간
     - 비유: 모델 파라미터 = 머릿속 지식 / KV Cache = 책상 위에 펼쳐 놓은 공책, 책, 참고자료

2. **왜 문제가 되나?**
   - KV Cache 크기 = 레이어 수 × 헤드 수 × 헤드 차원 × 토큰 수
   - 토큰이 늘수록 선형적으로 증가
   - 작업이 길어질수록, 대화가 많아질수록 책상이 꽉꽉 차듯 메모리가 쌓임
   - 실제 수치 (논문 기준)
     - Llama 3.1 8B: 토큰 1,000개당 128 MiB
     - Llama 3.3 70B: 토큰 1,000개당 320 MiB
     - 실사례: 1,000줄짜리 코드 파일(약 10K 토큰) + Llama 3.3 70B → KV Cache만 약 1.6 GiB 발생
   - 에이전틱 AI 시대에는 더 심각
     - 코드 파일 여러 개, 문서 여러 개, 반복 대화 → 모델 자체가 작아도 KV Cache는 폭증
   - 저장 위치도 문제
     - HBM, LPDDR 등 고속 메모리에 계속 쌓아두는 것은 한계 존재

3. **딜레마 구조**
   - GPU에 그냥 유지 → 다른 사용자 서비스 불가
   - 버리기(Eviction) → 다음 요청 때 처음부터 재계산, 지연 급증
   - CPU/SSD로 옮기기(Offload) → 전송 오버헤드 발생, 속도 저하
   - 이것이 LLM 서빙의 **latency-throughput 딜레마**
   - KVTC는 이 딜레마를 "압축"으로 해결하려는 시도

4. **KV Cache 재사용이 중요한 시나리오**
   - 멀티턴 대화
   - 공통 시스템 프롬프트 공유
   - 코드 편집 세션
   - 긴 문서 반복 질의
   - 위 경우 캐시를 잘 보관하면 재계산 없이 빠른 응답 가능
   - KVTC의 목표: 이 캐시를 작게 압축해 더 오래, 더 싸게 보관

---

## 2. 기존 해결책과 한계

1. **Eviction 계열 (H2O, TOVA)**
   - 덜 중요한 토큰 캐시를 아예 버림
   - 구현은 간단하지만 정보 손실 발생
   - 긴 컨텍스트에서 성능 급격히 저하
   - 캐시 재사용 자체가 불가능

2. **Quantization 계열 (KIVI, GEAR)**
   - 저장 비트 폭을 줄임
   - 5× 수준이 현실적 한계
   - 그 이상 올리면 정확도 저하 심함

3. **SVD / Low-rank 계열 (xKV, SVDq)**
   - 행렬을 저차원으로 근사
   - 요청마다 SVD를 새로 계산해야 해서 비용이 큼

4. **CacheGen**
   - 전송용 압축에 특화
   - 최대 8.6× 수준에 그침

5. **공통적인 문제**
   - 단일 기법만 사용 → 압축률에 한계
   - 정확도와 압축률 간 트레이드오프가 가팔라 둘 다 잡기 어려움
   - 특히 eviction 계열은 재사용 자체가 불가능

---

## 3. KVTC란 무엇인가?

1. **한 줄 정의**
   - "간단하지만 아주 강력한 변환 코딩 방식" (논문 표현)
   - KV Cache를 단순 비트 절감이 아닌 복합 파이프라인으로 처리
   - 높은 압축률과 성능 보존을 동시에 달성하는 저장 중심 압축 기술

2. **타겟이 명확한 기술**
   - 지금 당장 매 토큰마다 쓰고 있는 KV를 압축하는 것이 아님
   - 잠깐 안 쓰고 있는 KV, 즉 재사용 가능성이 있는 Cold KV Cache를 압축해 보관
   - 비유: 냉장고에 넣어서 보관하는 개념
     - 핫 데이터: 지금 프라이팬 위에서 바로 쓰고 있는 KV
     - 콜드 데이터: 당장은 안 쓰지만 나중에 다시 꺼낼 KV → KVTC의 타겟

3. **JPEG에서 영감**
   - JPEG 압축 방식
     - DCT 변환 → 에너지를 소수 성분에 집중
     - 중요도 낮은 부분 양자화로 뭉뚱그림
     - 허프만 코딩으로 자주 나오는 패턴을 짧게 저장
   - KVTC도 동일한 구조
     - 차이점: 이미지의 공간적 중복 대신 어텐션 헤드 간 중복성을 활용

4. **핵심 특징**
   - 모델 가중치 전혀 변경하지 않음
   - 추론 중 생성된 KV Cache에만 적용
   - 캘리브레이션은 모델당 딱 한 번만 수행
     - 12B 모델 기준 H100 GPU에서 10분 이내 완료 (논문 실측)
     - 이후 모든 추론에 동일한 기저 행렬 재사용

---

## 4. 핵심 발견: 헤드 간 숨겨진 중복성

1. **발견 내용**
   - AI의 여러 어텐션 헤드들이 사실 비슷한 일을 하고 있었음
   - 비유: 서로 다른 부서 직원들이 알고 보니 다 똑같은 보고서를 쓰고 있던 것
   - KVTC가 높은 압축률을 낼 수 있는 가장 근본적인 이유

2. **실험으로 확인 (논문, Llama 3.1 8B 기준)**
   - 정렬 전
     - 헤드 간 코사인 유사도 대체로 0.2 이하
     - 겉으로는 그냥 지지직거리는 노이즈처럼 아무 의미 없어 보임
   - 직교 정렬(Procrustes problem) 적용 후
     - Key: 유사도 크게 증가
     - Value: 의미 있는 수준으로 상승
     - 선명하고 규칙적인 패턴이 마법처럼 나타남
   - 해석
     - 서로 다른 헤드들이 실제로는 같은 정보 공간을 공유
     - 초기 가중치 랜덤 초기화 때문에 다르게 회전된 방향을 바라보고 있을 뿐
     - 방향만 맞춰주면 공통 구조가 드러남 = 압축하기에 최적의 상태

3. **PCA 선택의 수학적 근거**
   - 데이터 행렬 A를 k개 성분으로 설명 가능하다면
   - A에 직교 변환 R을 적용한 B도 동일하게 k개 성분으로 설명 가능
   - 여러 헤드를 하나로 이어 붙인 뒤 PCA 적용 시 공통 부분 공간을 한 번에 학습 가능

---

## 5. KVTC 동작 원리 (3단계 파이프라인)

1. **Step 1: PCA 디코릴레이션 (Feature Decorrelation)**
   - 데이터에서 쓸데없는 군더더기는 걷어내고 핵심 패턴만 뽑아내는 과정
   - 서로 비슷한 정보가 겹쳐 있으면 먼저 정리해 더 잘 압축되게끔 만드는 단계
   - 구체적 처리 방식
     - 캘리브레이션 데이터로 모델의 KV Cache 수집
     - 여러 레이어, 여러 헤드의 Key/Value를 시간 축으로 이어 붙여 행렬 C 구성
     - 평균을 빼고 SVD 계산 → 기저 행렬 V 추출
     - 이후 모든 추론에서 V를 재사용해 KV Cache를 압축 친화적인 공간으로 투영
   - 핵심 포인트
     - PCA 변환 후 에너지가 앞쪽 성분에 집중
     - 뒤쪽 성분은 분산이 거의 없으므로 버리거나 극소 비트로 저장 가능
     - RoPE(회전 위치 임베딩)는 압축 전에 제거 → 위치 정보가 Key의 저차원 구조를 왜곡하기 때문

2. **Step 2: 어댑티브 퀀타이제이션 (Adaptive Quantization)**
   - 알짜배기 정보를 약간 뭉뚱그려 단순화하는 과정
   - FP16, FP8, FP4처럼 숫자를 표현할 때 비트를 몇 개 쓸 것이냐의 문제
   - 중요한 부분과 덜 중요한 부분을 나눠 유리하게 압축
   - 구체적 처리 방식
     - 각 성분에 비트를 얼마나 할당할지 동적 프로그래밍(DP)으로 최적화
     - 분산이 큰 앞쪽 성분 → 더 많은 비트 할당
     - 분산이 작은 뒤쪽 성분 → 더 적은 비트 할당
     - 아예 0비트를 할당해 버리는 성분도 상당수 발생
   - 기존 균일 양자화와의 차이
     - 기존: 모든 값을 동일한 비트 폭으로 일괄 축소
     - KVTC: 성분별로 비트 폭이 다름 → 정확도 유지하면서 압축률 향상
   - 그룹 단위 처리 (Microscaling 방식 차용)
     - 연속된 PCA 성분들을 묶어 그룹별 16비트 스케일링 팩터를 함께 저장
     - DP는 그룹당 비트 폭과 그룹 크기를 동시에 최적화

3. **Step 3: 엔트로피 코딩 (Entropy Coding)**
   - 다이어트한 데이터를 빈틈없이 최대한 효율적으로 꽉꽉 채워서 저장하는 과정
   - 자주 나오는 값은 짧게, 드문 값은 길게 저장 (허프만 코딩 방식)
   - 구체적 처리 방식
     - DEFLATE 알고리즘으로 양자화된 값들을 추가 압축
     - NVIDIA nvCOMP 라이브러리를 사용해 GPU에서 병렬로 실행
     - 무손실 압축으로 평균 약 1.23×의 추가 압축 달성 (논문 실측)
   - **DEFLATE란?**
     - ZIP 파일이나 PNG 이미지를 압축할 때 실제로 사용되는 바로 그 알고리즘
     - 두 가지 기법을 결합한 무손실 압축 방식
       - LZ77: 앞에서 이미 나왔던 패턴을 "몇 번째 위치에서 몇 글자"로 짧게 대체
         - 예시: "ABCABC" → "ABC" + (3칸 앞에서 3글자 복사)
       - 허프만 코딩: 자주 나오는 값은 짧은 코드로, 드문 값은 긴 코드로 저장
         - 예시: 영어에서 'e'는 짧은 코드, 'z'는 긴 코드
     - 무손실이므로 복원 시 원본과 100% 동일하게 복구 가능
     - KVTC에서의 역할
       - 양자화 후 남은 패턴적 중복을 추가로 제거
       - 평균 약 1.23× 추가 압축 → 작아 보이지만 이미 수십 배 압축된 상태에서의 추가 절감
       - nvCOMP 라이브러리로 GPU에서 병렬 실행 → 속도 저하 최소화
   - 전체 파이프라인 압축 기여도 (논문, Llama 3.1 8B 기준)
     - kvtc 8×: 양자화 약 9~10× + DEFLATE 1.1~1.2× 추가
     - kvtc 64×: 양자화 약 64× + DEFLATE 최대 1.4× 추가 → 최종 최대 88× 수준

---

## 6. 스마트 예외 처리: Sink & Sliding Window

1. **왜 모든 토큰을 똑같이 압축하면 안 되나?**
   - 무식하게 모든 걸 다 압축하는 방식이 아님
   - 특히 중요한 기억들은 압축하지 않고 그대로 남겨두는 스마트함 보유
   - Attention Sink 토큰
     - 문장 맨 앞의 초기 토큰들
     - 거의 모든 다른 토큰들이 여기에 어텐션을 집중
   - 최근 토큰들 (Sliding Window)
     - 바로 직전의 최신 토큰들
     - 현재 생성에 직접 영향을 미침
   - 이 토큰들을 압축하면 성능 크게 저하되거나 완전 붕괴

2. **Ablation 실험 결과 (논문, Llama 3.1 8B, 64× 압축)**
   - Attention sink 토큰 압축 시
     - 일부 태스크에서 성능 붕괴 수준의 저하 발생
   - 최근 128개 토큰 압축 시
     - LITM 같은 long context 태스크에서 심각한 성능 저하 발생

3. **KVTC의 선택**
   - 기본 설정
     - 가장 오래된 토큰 중 4개 (attention sink): 원본 그대로 유지
     - 가장 최근 128개 토큰 (sliding window): 원본 그대로 유지
   - 나머지 토큰들은 공격적으로 압축
   - 결과: 높은 압축률에서도 성능 유지 가능

---

## 7. 실험 설계

1. **테스트한 모델**
   - Llama 3.1 8B, Llama 3.3 70B Instruct
   - MN-Minitron 8B, Mistral NeMo 12B
   - DeepSeek R1-distilled Qwen 2.5 (1.5B, 7B)
   - 규모: 1.5B ~ 70B / base, instruct, reasoning 모두 포함

2. **비교 대상 (Baseline)**
   - Quantization 계열: KIVI(2-bit), GEAR(2-bit), FP8
   - Eviction 계열: H2O, TOVA
   - SVD 계열: xKV
   - 고급 토큰 eviction: DMS (reasoning 모델 비교용)

3. **평가 태스크**
   - 수학/상식: GSM8K, MMLU, MATH-500
   - 추론: AIME 2024/2025, LiveCodeBench
   - Long context: QASPER, LostInTheMiddle(LITM), RULER-VT, NIAH
   - 멀티턴 시뮬레이션: 일정 토큰마다 압축/복원을 반복하는 방식으로 실제 대화 환경 모사

---

## 8. 실험 결과

1. **핵심 수치: 20배 압축, 성능은 그대로**
   - 가장 중요한 숫자: 평균 **20×**
   - 모델의 성능(똑똑함)은 거의 그대로인데 차지하는 메모리 공간을 1/20로 축소
   - MMLU 기준 (논문, Llama 3.1 8B)
     - 압축 전: 60.5점
     - 20배 압축 후: 60.1점
     - 차이: 0.4점 → 사실상 성능 손실 없음

2. **일반 모델 결과 (8B~12B 규모)**
   - kvtc 16× (실제 약 20× 압축): vanilla 대비 거의 모든 태스크에서 1점 이내 차이 유지
   - kvtc 32×에서도 일부 태스크에서 오히려 소폭 높은 경우도 존재
   - 기존 방법 비교
     - GEAR / KIVI (5×): GSM8K, LITM에서 이미 성능 저하 징후 포착
     - H2O: LITM에서 20.2점 (Vanilla 99.4점 대비)
     - TOVA: LITM에서 1.2점 → 사실상 사용 불가 수준
   - MN-Minitron 8B, Mistral NeMo 12B도 패턴 동일

3. **Reasoning 모델 결과 (DeepSeek R1 Qwen 2.5)**
   - Qwen 2.5 R1 7B, AIME 2024 기준
     - Vanilla: 50.9점 / kvtc 8×: 52.5점 → 소폭 높음
   - LiveCodeBench 기준
     - Vanilla: 36.7점 / kvtc 8×: 36.5점 → 사실상 동일
   - DMS (고급 토큰 eviction)와 비교
     - kvtc가 동등하거나 더 높은 성능 달성
     - 압축률은 kvtc가 훨씬 높음
     - 논문 언급: 둘을 결합하면 더 낮은 KV Cache 풋프린트 달성 가능

4. **대형 모델 결과 (Llama 3.3 70B)**
   - 4개의 GPU에 pipeline parallel로 분산하여 실험
   - MATH-500 기준
     - Vanilla: 75.6점 / kvtc 10×: 74.4점 / kvtc 20×: 72.6점
     - 성능 저하폭이 1.5 × 표준오차 이내 → 통계적으로 유의미한 차이 아님
   - NIAH, LITM 태스크: 모든 압축 설정에서 100점 유지

5. **압축률 요약 (논문, Llama 3.1 8B 기준)**

   | 설정 | 실제 압축률 범위 |
   |------|----------------|
   | kvtc 8× | 9~10× |
   | kvtc 16× | 18~22× |
   | kvtc 32× | 34~44× |
   | kvtc 64× | 60~88× |

   - DEFLATE의 실제 압축 효과가 데이터에 따라 달라지므로 범위로 표시

---

## 9. 지연시간(Latency) 분석

1. **압축/복원 소요 시간 (논문, Mistral NeMo 12B, H100, bfloat16 기준)**

   | 구성 | 압축 전체 | 복원 전체 |
   |------|----------|----------|
   | 배치 8, 컨텍스트 8K | 379ms | 267ms |
   | 배치 2, 컨텍스트 16K | 194ms | 143ms |

2. **핵심 비교: 재계산 vs KVTC 복원 (TTFT 기준)**

   | 구성 | Vanilla 재계산 | KVTC 복원 | 차이 |
   |------|--------------|----------|------|
   | 배치 8, 8K | 3,098ms | 380ms | 약 8× 빠름 |
   | 배치 2, 16K | 1,780ms | 208ms | 약 8.6× 빠름 |

3. **해석**
   - 압축 오버헤드가 있더라도 재계산 비용보다 훨씬 저렴
   - 버리고 다시 계산하는 것보다 압축 보관 후 복원이 압도적으로 빠름
   - AI가 멍때리는 시간(TTFT)을 최대 8× 이상 단축 가능

---

## 10. AI 메모리 산업에 미치는 영향

1. **단기적 영향**
   - 클라우드 AI 환경
     - 동일한 GPU 메모리로 더 많은 동시 사용자 서비스 가능
     - 더 긴 문맥 처리 또는 더 높은 활용률 달성 가능
     - KV Cache 전송량 감소 → 네트워크 병목 완화
   - 온디바이스 AI 환경
     - 스마트폰, 노트북 등 메모리 제한 환경에서 효과 큼
     - 컨텍스트 한계로 인한 세션 중단 및 초기화 문제 완화
     - 더 큰 모델을 로컬에서 현실적으로 구동 가능
     - 작은 모델로도 더 길고 복잡한 멀티턴 에이전트 AI 구현 가능

2. **메모리 계층화(Tiering) 관점**
   - 현재 업계 방향: 메모리를 단일 구조가 아닌 다층 구조로 운용
     - HBM → DRAM → SSD 계층별 역할 분담
     - NVIDIA 루비 플랫폼: HBM보다 높은 대역폭의 SRAM을 추가한 LPX 렉 도입
   - KV Cache도 성격에 따라 분화
     - Hot KV: 매 토큰 생성마다 즉시 참조하는 KV → 고속 메모리 유지
     - Cold KV: 당장 안 쓰지만 재사용 가능성 있는 KV → KVTC로 압축 보관
   - KVTC는 이 계층형 메모리 전략의 핵심 빌딩 블록

3. **장기적 관점**
   - 절약된 메모리만큼 더 긴 컨텍스트, 더 좋은 모델, 더 많은 사용자로 이어질 가능성
   - 메모리가 필요 없어지는 것이 아니라 더 똑똑하게 사용되는 방향으로 발전
   - 미래 LLM 서빙 경쟁은 모델 크기보다 작업 기억 관리 효율의 경쟁으로 이동 중

---

## 11. 한계 및 향후 연구

1. **현재 한계**
   - 압축/복원 오버헤드 존재
     - 재계산보다는 빠르지만 현재는 단순 구현 기준
     - 커널 fusion + 계층적 PCA 적용 시 비용 절감 가능 (논문 직접 언급)
   - 실험 환경과 실제 서비스 간의 괴리 가능성
     - 벤치마크 기반 멀티턴 시뮬레이션
     - 실제 트래픽 분포나 상호작용 패턴과 차이 발생 가능
   - 캘리브레이션 데이터 규모 한계
     - 현재 약 20만 토큰 기준으로 실험
     - 더 큰 규모로의 확장은 향후 과제
   - 성능 대리 지표의 한계
     - Frobenius norm 재구성 오차를 대리 지표로 사용
     - 실제 태스크 성능과 항상 일치하지 않을 가능성 존재

2. **향후 연구 방향**
   - 압축된 PCA 공간에서 직접 추론 (복원 없이 연산)
   - 토큰 eviction 방법(DMS 등)과 결합 → 더 낮은 KV Cache 풋프린트 달성
   - Multi-head Latent Attention 구조에서 latent state 압축 적용 가능성 탐색
   - 70B 이상 초대형 모델에서의 추가 검증

---

## 12. 최종 요약 및 의의

1. **한 줄 요약**
   - 모델 변경 없이 재사용 가능한 Cold KV Cache를 PCA 변환 + 적응형 양자화 + 엔트로피 코딩의 3단계로
   - 평균 20×, 특정 조건에서 40×+ 압축하면서 성능 저하를 최소화하는 저장 중심 압축 기법

2. **현장에서 갖는 의미**
   - 클라우드 AI
     - 동일한 GPU 메모리로 더 많은 동시 사용자 서비스 가능
     - 재계산 대비 최대 8× 빠른 TTFT 회복
     - CPU/SSD 오프로딩 시 전송량 비례 절감
     - 더 긴 컨텍스트 처리 또는 더 높은 서버 활용률 달성 가능
   - 온디바이스 AI
     - 스마트폰, 노트북 등 메모리 제한 환경에서 더 큰 모델 구동 가능
     - 더 긴 멀티턴 대화 및 에이전트 AI 구현 가능
     - 컨텍스트 한계로 인한 세션 중단 및 초기화 문제 완화
   - 사용자 관점
     - AI 챗봇 응답 속도 향상, AI가 멍때리는 시간 감소
     - AI 사용 비용 절감 → 더 많은 사람이 AI 혜택을 누릴 수 있는 환경 조성

3. **앞으로의 방향성: 메모리 계층화의 시대**
   - 지금까지의 AI 경쟁이 "모델을 얼마나 잘 만드느냐"였다면
   - 앞으로는 "작업 중 메모리를 얼마나 효율적으로 쓰느냐"의 경쟁으로 이동
   - KV Cache를 단일 덩어리로 보는 시대는 끝
     - Hot KV: 매 토큰 생성마다 즉시 참조 → 고속 메모리(HBM)에 유지
     - Warm KV: 잠시 대기 중인 KV → 중간 계층(DRAM)으로 이동
     - Cold KV: 당장 안 쓰지만 재사용 가능성 있는 KV → KVTC로 압축해 SSD/스토리지 보관
   - NVIDIA 루비 플랫폼처럼 하드웨어 레벨에서도 메모리 계층화가 이미 진행 중
   - KVTC는 이 계층형 메모리 전략에서 Cold tier를 담당하는 핵심 기술

4. **활용 가능성 확장**
   - 에이전틱 AI 시대에 특히 중요
     - 코드 파일 여러 개, 문서 여러 개, 반복 작업이 쌓이는 환경에서 KV Cache 폭증 문제 직접 완화
     - 긴 세션을 유지하면서도 메모리 부담 없이 에이전트 AI 구현 가능
   - 다른 압축 기술과의 결합 가능성
     - 토큰 eviction(DMS 등)과 KVTC를 함께 사용하면 더 낮은 풋프린트 달성 가능
     - Hot KV는 별도 경량화 기술로, Cold KV는 KVTC로 처리하는 역할 분담 구조 가능
   - 절약된 메모리의 재투자
     - 메모리가 줄어든다 = 메모리가 필요 없어진다는 의미가 아님
     - 절약된 만큼 더 긴 컨텍스트, 더 좋은 모델, 더 많은 사용자, 멀티모달리티 확장으로 이어질 것
     - 결국 AI를 더 똑똑하게, 더 많이 쓸 수 있는 기반이 되는 기술

5. **올바른 이해 포인트**
   - attention 연산 자체를 빠르게 만드는 기술이 아님
   - 잠깐 안 쓰는 KV Cache를 버리지 않고 작게 접어 보관하는 기술
   - 단기적으로는 메모리 효율 향상이지만 장기적으로는 AI 활용 범위 자체를 확장시키는 기술
   - KVTC 하나로 모든 것이 해결되는 게 아니라 계층형 메모리 아키텍처의 퍼즐 한 조각